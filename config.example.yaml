# CLIProxyAPI plugin configuration for key-bind.
# Merge this under the top-level `plugins:` key in your CLIProxyAPI config.yaml.

plugins:
  enabled: true
  dir: "plugins"
  configs:
    key-bind:
      enabled: true
      bindings: []  # Managed by the key-bind UI and persisted in this config file.
